import React from 'react';
import BattleResultScreen from './BattleResultScreen';

const LeaderboardScreen = (props) => {
  return <BattleResultScreen {...props} />;
};

export default LeaderboardScreen;
